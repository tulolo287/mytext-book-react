{
  "name": "mytext-book-react",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:client": "yarn workspace client run dev",
    "dev:server": "yarn workspace server run dev",
    "dev": "yarn run dev:server && yarn run dev:client",
    "bootstrap": "lerna exec yarn install"
  },
  "devDependencies": {
    "lerna": "^8.1.6"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "sequelize-auto": "^0.8.8"
  }
}
